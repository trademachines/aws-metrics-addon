{
  "name": "aws-metrics-addon",
  "description": "Adds additional metrics to AWS CloudWatch that can be utilized for monitoring, alarming or auto-scaling",
  "license": "MIT",
  "devDependencies": {
    "aws-sdk": "^2.4.14",
    "coveralls": "^2.11.12",
    "istanbul": "^0.4.5",
    "jasmine": "^2.5.0",
    "jasmine-expect": "^3.0.1",
    "jasmine-spec-reporter": "^2.7.0"
  },
  "dependencies": {
    "lodash": "^4.15.0",
    "neo-async": "^2.0.0"
  },
  "scripts": {
    "test": "node_modules/.bin/istanbul cover node_modules/.bin/jasmine",
    "coveralls": "cat ./coverage/lcov.info | node_modules/.bin/coveralls && rm -rf ./coverage"
  }
}
